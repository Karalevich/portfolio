import styled from '@emotion/styled'
import Input from '../Custom/Input/Input'

export const SignFormInput = styled(Input)(({}) => ({
  marginBottom: '0.5rem',
  '.MuiInputBase-root': {
    width: '100%',
  },
}))
